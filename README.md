# NexusHub - 个人导航仪表板

一个现代化的个人导航仪表板,支持内外网切换、用户管理、素材库等功能。

## ✨ 特性

- 🎨 **现代化 UI** - 基于 React + TailwindCSS,美观流畅
- 🔐 **用户认证** - 支持多用户管理和访客模式
- 🌐 **内外网切换** - 一键切换内网/外网地址
- 📦 **素材库管理** - 图标和壁纸本地化存储
- 🔍 **自动获取图标** - 智能抓取网站图标,支持带端口的内网地址
- 🎯 **拖拽排序** - 服务卡片支持拖拽排序和分类
- 🌈 **主题定制** - 自定义颜色、壁纸、模糊度等
- 📱 **响应式设计** - 完美适配各种屏幕尺寸
- 🐳 **Docker 支持** - 一键部署
- 🚀 **离线运行** - 所有依赖本地化,无需外网

## 📁 项目结构

```
nexushub/
├── src/                    # 前端源代码
│   ├── components/         # React 组件
│   ├── App.tsx            # 主应用组件
│   ├── main.tsx           # 入口文件
│   ├── types.ts           # TypeScript 类型
│   ├── constants.ts       # 常量配置
│   └── utils.ts           # 工具函数
├── server/                # 后端代码
│   └── index.js           # Express 服务器
├── scripts/               # 构建脚本
│   └── download_assets.bat # 下载外部资源
├── public/                # 静态资源
│   ├── vendor/            # 本地化的第三方库
│   └── uploads/           # 用户上传的素材
├── data/                  # 数据存储
│   └── data.json          # 配置和服务数据
└── dist/                  # 构建输出
```

## 🚀 快速开始

### 前置要求

- Node.js 18+ 
- npm 或 yarn

### 开发模式

1. **克隆项目**
   ```bash
   git clone <repository-url>
   cd nexushub
   ```

2. **安装依赖**
   ```bash
   npm install
   ```

3. **下载外部资源**(首次运行)
   ```bash
   scripts\download_assets.bat
   ```

4. **启动开发服务器**
   ```bash
   npm run dev:all
   ```
   - 前端: http://localhost:3000
   - 后端 API: http://localhost:3001

### 生产构建

1. **构建前端**
   ```bash
   npm run build
   ```

2. **启动生产服务器**
   ```bash
   npm run dev:server
   ```

## 🐳 Docker 部署

### 使用 Docker Compose(推荐)

1. **构建镜像**
   ```bash
   docker-compose build
   ```

2. **启动服务**
   ```bash
   docker-compose up -d
   ```

3. **访问应用**
   - 应用: http://localhost:3001

### 使用 Dockerfile

```bash
# 构建镜像
docker build -t nexushub .

# 运行容器
docker run -d \
  -p 3001:3001 \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/public/uploads:/app/public/uploads \
  --name nexushub \
  nexushub
```

## 📝 使用说明

### 首次使用

1. 访问应用后,系统会提示设置管理员账户
2. 输入用户名和密码(至少 4 位)
3. 登录后即可开始配置

### 添加服务

1. 点击右上角的 **+** 按钮
2. 填写服务信息:
   - **名称**: 服务名称
   - **描述**: 简短描述
   - **内网地址**: 内网访问地址(可选)
   - **外网地址**: 外网访问地址(必填)
   - **分类**: 服务分类
3. 配置图标:
   - **自动获取**: 输入网址自动抓取图标
   - **图片链接**: 直接输入图片 URL
   - **本地上传**: 上传本地图片
   - **从图库选择**: 从已保存的素材中选择

### 系统设置

点击右上角的齿轮图标进入设置:

- **界面外观**: 自定义颜色、卡片大小等
- **壁纸设置**: 上传或使用在线壁纸
- **用户管理**: 添加/删除用户(仅管理员)
- **素材管理**: 管理图标和壁纸库
- **数据管理**: 导出/导入配置

### 数据持久化

所有数据存储在以下位置:
- `data/data.json` - 配置和服务数据
- `public/uploads/` - 用户上传的素材

## 🛠️ 开发

### 可用脚本

- `npm run dev` - 启动前端开发服务器
- `npm run dev:server` - 启动后端服务器
- `npm run dev:all` - 同时启动前后端
- `npm run build` - 构建生产版本
- `npm run preview` - 预览生产构建

### 技术栈

**前端**
- React 19
- TypeScript
- TailwindCSS
- Vite
- Lucide Icons

**后端**
- Node.js
- Express
- Multer (文件上传)

## 📦 API 端点

### 数据管理
- `GET /api/data` - 获取配置和服务数据
- `POST /api/data` - 更新配置和服务数据

### 素材管理
- `GET /api/assets` - 获取素材列表
- `POST /api/assets/upload` - 上传素材
- `POST /api/assets/upload-from-url` - 从 URL 下载素材
- `DELETE /api/assets/:filename` - 删除素材

### 图标获取
- `GET /api/fetch-icon-candidates?url=<url>` - 获取网站图标候选列表
- `GET /api/proxy-image?url=<url>` - 代理图片请求

## 🤝 贡献

欢迎提交 Issue 和 Pull Request!

## 📄 许可证

MIT License

## 🙏 致谢

- [React](https://react.dev/)
- [Vite](https://vitejs.dev/)
- [TailwindCSS](https://tailwindcss.com/)
- [Lucide Icons](https://lucide.dev/)
